services:
  smarteros-landing:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: smarteros-landing
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - smarteros-net

networks:
  smarteros-net:
    driver: bridge
