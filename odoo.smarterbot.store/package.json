{
  "name": "odoo-smarterbot-store",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "vercel-build": "cd landing && (npm ci || npm install) && NODE_ENV=production npm run build",
    "build": "npm run vercel-build"
  }
}
