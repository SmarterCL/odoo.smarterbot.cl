services:
  smarteros-landing:
    build: .
    container_name: smarteros-landing
    restart: unless-stopped
    env_file:
      - .env.production
    ports:
      - "3000:3000"
    networks:
      - smarteros-net

networks:
  smarteros-net:
    driver: bridge
